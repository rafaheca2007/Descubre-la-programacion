int Jugar = 1; ;
int Repinta = 1;
int Tamano = 35;
int Puntos = 0;
int PosX = 0;
int PosY = 0;

void main(){
  size(800,600);
  background(0,0,0);
}

void draw(){
  if((Jugar == 1) && (Repinta == 1));
}

void elemento(){
  background(0,0,0);
  for(int i = 0; i<=Puntos; i++)
    {
			fill(25,25,25);
    	ellipse(50,750, 300,20);
      ellipse(50,750, 300,20); 

  }
}

